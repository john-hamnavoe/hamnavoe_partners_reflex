<!DOCTYPE html>
<html class="antialiased">
  <%= render "shared/head" %>

  <body class="font-sans font-normal leading-normal text-gray-800 bg-gray-100">
    <%= render "shared/flash_notice" %>
    <%= render "shared/header" %>

    <main class="px-4 lg:px-6">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>
